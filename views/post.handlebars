<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/api/posts/{{id}}">{{title}}</a>
    </h2>

    <p>Created by {{user.username}} on {{createdAt}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post_text}}
    </p>
  </div>
</div>

<div class="row">
  <div class="col-md-6">

    <form class="form comment-form">
      <div class="form-group">
        <label for="comment-added"> Comments: </label>
        <input class="form-input" type="text" id="comment-added" />
      </div>
      <div class="form-group">
          <button class="btn" type="submit">add comment</button>
      </div>
    </form>
  </div>
  <div class="col-md-6">